name: multitally
description: A camera tally app live video mixers.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  wakelock: ^0.6.2
  shared_preferences: ^2.0.17
  async: ^2.11.0
  cancellation_token: ^2.0.1

flutter:
  uses-material-design: true
